.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install period_ranges

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv period_ranges
    $ pip install period_ranges
